win10:
    script:
        - "git submodule init"
        - "git submodule update --recursive"
        - "mkdir build"
        - "cd build"
        - "cmake -G \"Visual Studio 14 Win64\" .."
        - "cmake --build ."
    tags:
        - win10

win7:
    script:
        - "git submodule update --recursive"
        - "mkdir build"
        - "cd build"
        - "cmake -G \"Visual Studio 14 Win64\" .."
        - "cmake --build ."
    tags:
        - win7

linux:
    script:
        - "git submodule update --init"
        - "mkdir build"
        - "cd build"
        - "cmake .."
        - "cmake --build ."
    tags:
        - lin

mac:
    script:
        - "git submodule update --init"
        - "mkdir build"
        - "cd build"
        - "cmake .."
        - "cmake --build ."
    tags:
        - mac
